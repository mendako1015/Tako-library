// paste tp main.

int N, Q;
cin >> N >> Q;
int q, a, b;

for(int i = 0; i < N; i++) {
  cin >> q;
  if(q == 0) {
    cin >> a >> b;
  }
  if(q == 1) {
    cin >> a;
  }
  if(q == 2) {
    cin >> a;
  }
}
